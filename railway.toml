[build]
builder = "dockerfile"

[deploy]
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"

[env]
PORT = "8000"
PYTHONUNBUFFERED = "1"
DEBUG = "false"
LOG_LEVEL = "info"